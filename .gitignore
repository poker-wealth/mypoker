# Dependencies / build output (per-package .gitignore files also apply)
node_modules/
dist/
coverage/

# Env / secrets — NEVER commit
.env
.env.local
**/.env
!**/.env.example

# Extracted spec text (regenerate with extract_docx.py; source .docx are tracked)
docs_extracted/

# Editor / OS
.DS_Store
*.log
.idea/
.vscode/
